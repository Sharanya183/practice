def sr(n):
    low = 0
    high = n
    while low<=high:
        mid = (low+high)/2
        if mid * mid == n:
            return mid
        if mid * mid > n:
            high = mid + 1
        else:
            low = mid - 1
n = float(input("enter "))
print(sr(n))
